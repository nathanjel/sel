// GENERATED by tools/gen-sql-map.mjs from sql/dialects/*.json — do not edit.
// Regenerate with: node tools/gen-sql-map.mjs
// The format is normative in sql/MAP.md; the design is in docs/SQL-TRANSLATION.md.
//
// The dialect documents as written, parents first, for the replay check.
// 
// Not library data and not shipped: this is the input the generator read, kept
// in the harness so the check can register it through the public API and diff
// the result against the flattened map beside it. sql/MAP.md §4.5¼ is the rule
// it proves -- anything the shipped map contains, an application could have
// registered -- and that is what lets a host emit its map as code rather than
// ship a file to parse.
//
// Harness, not library: this lives under bin/ because it is test data, and
// nothing an application links carries it. Every call below goes through the
// PUBLIC registration API, which is the whole point -- if the shipped table
// holds something these constructors cannot say, this file does not compile.

#include "map_replay.hpp"

namespace sel::sqlt {

int replay_register() {
  int calls = 0;
  Map::define_dialect("ansi~replay",
                      DialectSpec::root("1999").target(false).lexical("identQuote", "\"").lexical("identEscape", "\"\"").lexical("textQuote", "'").lexical_escapes("textEscape", {{"'", "''"}}).lexical("true", "TRUE").lexical("false", "FALSE").lexical("binaryLiteral", "X'{hex}'").lexical("numericLiteral", "{0}").lexical("textCollate", "").lexical("textCharset", std::nullopt).lexical("numericCast", "CAST({0} AS DECIMAL(38,10))").lexical("binaryCast", "CAST({0} AS BINARY)").lexical("isTrue", "({0}) IS TRUE").lexical("isNotTrue", "({0}) IS NOT TRUE").lexical("placeholder", "?").lexical("textCast", "CAST({0} AS CHAR)"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "+", EntrySpec::tpl("({0} + {1})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "-", EntrySpec::tpl("({0} - {1})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "*", EntrySpec::tpl("({0} * {1})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "/", EntrySpec::tpl("({0} / {1})", "NUM").caveat("division-scale"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "%", EntrySpec::tpl("MOD({0}, {1})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "NEG", EntrySpec::tpl("(-{0})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "&", EntrySpec::variants({{"text", "({0} || {1})"}}, "@concat").caveat("concat-null"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "AND", EntrySpec::tpl("({0} AND {1})", "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "OR", EntrySpec::tpl("({0} OR {1})", "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "NOT", EntrySpec::tpl("(NOT {0})", "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "XOR", EntrySpec::tpl("(({0}) <> ({1}))", "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "==", EntrySpec::variants({{"num", "({0} = {1})"}, {"coerce", "({numericCast:0} = {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "!=", EntrySpec::variants({{"num", "({0} <> {1})"}, {"coerce", "({numericCast:0} <> {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "<", EntrySpec::variants({{"num", "({0} < {1})"}, {"coerce", "({numericCast:0} < {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "<=", EntrySpec::variants({{"num", "({0} <= {1})"}, {"coerce", "({numericCast:0} <= {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              ">", EntrySpec::variants({{"num", "({0} > {1})"}, {"coerce", "({numericCast:0} > {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              ">=", EntrySpec::variants({{"num", "({0} >= {1})"}, {"coerce", "({numericCast:0} >= {numericCast:1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$==", EntrySpec::variants({{"text", "({0} = {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$!=", EntrySpec::variants({{"text", "({0} <> {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$<", EntrySpec::variants({{"text", "({0} < {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$<=", EntrySpec::variants({{"text", "({0} <= {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$>", EntrySpec::variants({{"text", "({0} > {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "$>=", EntrySpec::variants({{"text", "({0} >= {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "EQL", EntrySpec::variants({{"text", "({0} = {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "IN", EntrySpec::variants({{"scalar", "({0} = {1})"}}, "BOOL"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "BAND", EntrySpec::withdraw("SQL's & is an integer operator; SEL's BAND is a byte-string operator over BIN of equal length, and no portable spelling of that exists"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "BOR", EntrySpec::withdraw("SQL's | is an integer operator; SEL's BOR is a byte-string operator over BIN of equal length, and no portable spelling of that exists"));
  ++calls;
  Map::define("ansi~replay", Section::Ops,
              "BXOR", EntrySpec::withdraw("SQL's ^ is an integer operator; SEL's BXOR is a byte-string operator over BIN of equal length, and no portable spelling of that exists"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "LEN", EntrySpec::tpl("CHAR_LENGTH({0})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "SUBSTR", EntrySpec::by_count({{"2", "SUBSTRING({0} FROM {1})"}, {"3", "SUBSTRING({0} FROM {1} FOR {2})"}}, "TEXT"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "UPPER", EntrySpec::tpl("UPPER({0})", "TEXT").caveat("unicode-case"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "LOWER", EntrySpec::tpl("LOWER({0})", "TEXT").caveat("unicode-case"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "TRIM", EntrySpec::tpl("TRIM(BOTH FROM {0})", "TEXT").caveat("trim-charset"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "LTRIM", EntrySpec::tpl("TRIM(LEADING FROM {0})", "TEXT").caveat("trim-charset"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RTRIM", EntrySpec::tpl("TRIM(TRAILING FROM {0})", "TEXT").caveat("trim-charset"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "ABS", EntrySpec::tpl("ABS({0})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "CEIL", EntrySpec::tpl("CEIL({0})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "FLOOR", EntrySpec::tpl("FLOOR({0})", "NUM"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "POWER", EntrySpec::tpl("POWER({0}, {1})", "NUM").caveat("power-float"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "LEFT", EntrySpec::withdraw("no ANSI spelling; each server has its own, so this is set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RIGHT", EntrySpec::withdraw("no ANSI spelling; each server has its own, so this is set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "FIND", EntrySpec::withdraw("ANSI POSITION exists but takes no start offset and spells its arguments differently in every server"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "REPLACE", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "SPLIT", EntrySpec::withdraw("yields a list, and a SQL expression is a scalar"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "BACKWARDS", EntrySpec::withdraw("no ANSI spelling; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "REPEAT", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "PADL", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "PADR", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "CHAR", EntrySpec::withdraw("not ANSI; the code-point-to-character spelling differs per server and per charset"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "CODE", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "SIGN", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "TRUNC", EntrySpec::withdraw("not ANSI; ROUND-toward-zero is spelled differently in every server"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "ROUND", EntrySpec::withdraw("not ANSI as a two-argument scale function; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "MIN", EntrySpec::withdraw("SEL's MIN is a scalar over its arguments, not a column aggregate; the scalar form is LEAST or GREATEST and is not ANSI"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "MAX", EntrySpec::withdraw("SEL's MAX is a scalar over its arguments, not a column aggregate; the scalar form is LEAST or GREATEST and is not ANSI"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "ISNUM", EntrySpec::withdraw("would need a regex or a failure-tolerant cast, and neither is ANSI"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "BLEN", EntrySpec::withdraw("not ANSI; octet length of a text value depends on the server's charset handling"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "TO_UTF8", EntrySpec::withdraw("SEL's TEXT/BIN distinction has no SQL counterpart; a cast to a binary type is per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "FROM_UTF8", EntrySpec::withdraw("SEL's TEXT/BIN distinction has no SQL counterpart; a cast from a binary type is per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "TO_HEX", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "FROM_HEX", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "ENCODE_BASE64", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "DECODE_BASE64", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "CRC32", EntrySpec::withdraw("not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "BTL", EntrySpec::withdraw("yields a list, and a SQL expression is a scalar"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "LTB", EntrySpec::withdraw("takes a list, and a SQL expression is a scalar"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RMATCH", EntrySpec::withdraw("regular expressions are not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RFIND", EntrySpec::withdraw("regular expressions are not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RREPLACE", EntrySpec::withdraw("regular expressions are not ANSI; set per dialect"));
  ++calls;
  Map::define("ansi~replay", Section::Funcs,
              "RGROUPS", EntrySpec::withdraw("yields a list, and a SQL expression is a scalar"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "case", EntrySpec::skeleton("CASE {branches} ELSE {else} END"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "caseBranch", EntrySpec::skeleton("WHEN {cond} THEN {then}"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "all", EntrySpec::skeleton("NOT EXISTS (SELECT 1 FROM {from} WHERE {corr} AND ({body}) IS NOT TRUE)"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "any", EntrySpec::skeleton("EXISTS (SELECT 1 FROM {from} WHERE {corr} AND ({body}) IS TRUE)"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "sum", EntrySpec::skeleton("(SELECT COALESCE(SUM({body}), 0) FROM {from} WHERE {corr})"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "count", EntrySpec::skeleton("(SELECT COUNT(*) FROM {from} WHERE {corr})"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "inRelation", EntrySpec::skeleton("(({needle} IN (SELECT {body} FROM {from} WHERE {corr})) IS TRUE)"));
  ++calls;
  Map::define("ansi~replay", Section::Skel,
              "join", EntrySpec::withdraw("LISTAGG is SQL:2016 and is spelled differently by every server that has it"));
  ++calls;
  Map::define_dialect("mysql-family~replay",
                      DialectSpec::extending("ansi~replay").version("0").target(false).lexical("identQuote", "`").lexical("identEscape", "``").lexical("textQuote", "'").lexical_escapes("textEscape", {{"'", "''"}, {"\\", "\\\\"}}).lexical("textCollate", " COLLATE utf8mb4_bin").lexical("textCharset", "utf8mb4").lexical("numericCast", "CAST({0} AS DECIMAL(65,10))").lexical("numericGuard", "CASE WHEN ({0} REGEXP '\\\\A-?[0-9]+(\\\\.[0-9]+)?\\\\z') THEN CAST({0} AS DECIMAL(65,10)) ELSE NULL END").lexical("textCast", "CAST({0} AS CHAR)"));
  ++calls;
  Map::define("mysql-family~replay", Section::Ops,
              "*", EntrySpec::tpl("({0} * {1})", "NUM").caveat("scale-limit"));
  ++calls;
  Map::define("mysql-family~replay", Section::Ops,
              "&", EntrySpec::variants({{"text", "CONCAT({0}, {1})"}, {"bin", "CONCAT({0}, {1})"}}, "@concat").caveat("concat-null"));
  ++calls;
  Map::define("mysql-family~replay", Section::Ops,
              "XOR", EntrySpec::tpl("({0} XOR {1})", "BOOL"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "LEFT", EntrySpec::tpl("LEFT({0}, {1})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "RIGHT", EntrySpec::tpl("RIGHT({0}, {1})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "SUBSTR", EntrySpec::by_count({{"2", "SUBSTRING({0}, {1})"}, {"3", "SUBSTRING({0}, {1}, {2})"}}, "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "FIND", EntrySpec::by_count({{"2", "INSTR({1}, {0})"}, {"3", "LOCATE({0}, {1}, {2})"}}, "NUM"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "REPLACE", EntrySpec::tpl("REPLACE({2}, {0}, {1})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "TRIM", EntrySpec::tpl("REGEXP_REPLACE({0}, '^[ \\\\t\\\\r\\\\n]+|[ \\\\t\\\\r\\\\n]+$', '')", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "LTRIM", EntrySpec::tpl("REGEXP_REPLACE({0}, '^[ \\\\t\\\\r\\\\n]+', '')", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "RTRIM", EntrySpec::tpl("REGEXP_REPLACE({0}, '[ \\\\t\\\\r\\\\n]+$', '')", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "BACKWARDS", EntrySpec::tpl("REVERSE({0})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "REPEAT", EntrySpec::tpl("REPEAT({0}, {1})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "PADL", EntrySpec::tpl("IF(CHAR_LENGTH({0}) >= {1}, {0}, LPAD({0}, {1}, {2}))", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "PADR", EntrySpec::tpl("IF(CHAR_LENGTH({0}) >= {1}, {0}, RPAD({0}, {1}, {2}))", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "CHAR", EntrySpec::withdraw("CHAR(n USING utf8mb4) reads n as a byte sequence in that charset, not as a code point: CHAR(233 USING utf8mb4) is NULL because 0xE9 alone is not valid UTF-8, where SEL says the character is e-acute"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "CODE", EntrySpec::withdraw("ORD returns the first character's bytes read as an integer, not its code point; SEL's CODE is a code point"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "SIGN", EntrySpec::tpl("SIGN({0})", "NUM"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "TRUNC", EntrySpec::tpl("TRUNCATE({0}, 0)", "NUM"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "ROUND", EntrySpec::tpl("ROUND({0}, {1})", "NUM"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "MIN", EntrySpec::by_count({{"1", "{0}"}, {"*", "LEAST({*})"}}, "NUM").caveat("numeric-scale"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "MAX", EntrySpec::by_count({{"1", "{0}"}, {"*", "GREATEST({*})"}}, "NUM").caveat("numeric-scale"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "ISNUM", EntrySpec::tpl("({0} REGEXP '\\\\A-?[0-9]+(\\\\.[0-9]+)?\\\\z')", "BOOL"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "BLEN", EntrySpec::tpl("LENGTH({binaryCast:0})", "NUM"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "TO_UTF8", EntrySpec::tpl("CAST({0} AS BINARY)", "BIN"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "FROM_UTF8", EntrySpec::tpl("CONVERT({binaryCast:0} USING {textCharset})", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "TO_HEX", EntrySpec::tpl("LOWER(HEX({binaryCast:0}))", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "FROM_HEX", EntrySpec::tpl("UNHEX({0})", "BIN"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "ENCODE_BASE64", EntrySpec::tpl("REPLACE(REPLACE(TO_BASE64({binaryCast:0}), CHAR(10), ''), CHAR(13), '')", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "DECODE_BASE64", EntrySpec::tpl("FROM_BASE64({0})", "BIN").caveat("input-laxity"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "CRC32", EntrySpec::tpl("LPAD(LOWER(HEX(CRC32({binaryCast:0}))), 8, '0')", "TEXT"));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "RFIND", EntrySpec::tpl("REGEXP_INSTR({1}{textCollate}, {0})", "NUM").caveat("regex-engine").arity(2, 2));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "RMATCH", EntrySpec::tpl("({1}{textCollate} REGEXP {0})", "BOOL").caveat("regex-engine").arity(2, 2));
  ++calls;
  Map::define("mysql-family~replay", Section::Funcs,
              "RREPLACE", EntrySpec::withdraw("SEL replacement syntax is $0-$9 and MariaDB's is \\1; rewriting one into the other is only possible when the replacement is a literal, which the map cannot express"));
  ++calls;
  Map::define("mysql-family~replay", Section::Skel,
              "join", EntrySpec::withdraw("GROUP_CONCAT does not specify an order without an ORDER BY, and a relation binding has no key to order by; SEL's JOIN concatenates in insertion order"));
  ++calls;
  Map::define_dialect("mysql~replay",
                      DialectSpec::extending("mysql-family~replay").version("8.4").target(true));
  ++calls;
  Map::define_dialect("postgresql~replay",
                      DialectSpec::extending("ansi~replay").version("15").target(true).lexical("identQuote", "\"").lexical("identEscape", "\"\"").lexical("textQuote", "'").lexical_escapes("textEscape", {{"'", "''"}}).lexical("true", "TRUE").lexical("false", "FALSE").lexical("binaryLiteral", "'\\x{hex}'::bytea").lexical("numericLiteral", "{0}").lexical("textCollate", " COLLATE \"C\"").lexical("textCast", "CAST({0} AS TEXT)").lexical("numericCast", "CAST({0} AS NUMERIC)").lexical("numericGuard", "CASE WHEN ({textCast:0} ~ '^-?[0-9]+(\\.[0-9]+)?$') THEN CAST({0} AS NUMERIC) ELSE NULL END").lexical("binaryCast", "convert_to(CAST({0} AS TEXT), 'UTF8')").lexical("isTrue", "(({0}) IS TRUE)").lexical("isNotTrue", "(({0}) IS NOT TRUE)").lexical("placeholder", "?"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "+", EntrySpec::tpl("({numericCast:0} + {numericCast:1})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "-", EntrySpec::tpl("({numericCast:0} - {numericCast:1})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "*", EntrySpec::tpl("({numericCast:0} * {numericCast:1})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "/", EntrySpec::tpl("({numericCast:0} / {numericCast:1})", "NUM").caveat("division-scale"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "%", EntrySpec::tpl("MOD({numericCast:0}, {numericCast:1})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "NEG", EntrySpec::tpl("(-{numericCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Ops,
              "&", EntrySpec::variants({{"text", "({textCast:0} || {textCast:1})"}, {"bin", "({0} || {1})"}}, "@concat").caveat("concat-null"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "LEN", EntrySpec::tpl("length({textCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "LEFT", EntrySpec::tpl("left({textCast:0}, CAST({1} AS INTEGER))", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "RIGHT", EntrySpec::tpl("right({textCast:0}, CAST({1} AS INTEGER))", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "FIND", EntrySpec::tpl("strpos({textCast:1}, {textCast:0})", "NUM").arity(2, 2));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "REPLACE", EntrySpec::tpl("replace({textCast:2}, {textCast:0}, {textCast:1})", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "TRIM", EntrySpec::tpl("btrim({textCast:0}, E' \\t\\r\\n')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "LTRIM", EntrySpec::tpl("ltrim({textCast:0}, E' \\t\\r\\n')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "RTRIM", EntrySpec::tpl("rtrim({textCast:0}, E' \\t\\r\\n')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "BACKWARDS", EntrySpec::tpl("reverse({textCast:0})", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "REPEAT", EntrySpec::tpl("repeat({textCast:0}, CAST({1} AS INTEGER))", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "PADL", EntrySpec::tpl("CASE WHEN length({textCast:0}) >= {1} THEN {textCast:0} ELSE lpad({textCast:0}, CAST({1} AS INTEGER), {textCast:2}) END", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "PADR", EntrySpec::tpl("CASE WHEN length({textCast:0}) >= {1} THEN {textCast:0} ELSE rpad({textCast:0}, CAST({1} AS INTEGER), {textCast:2}) END", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "CODE", EntrySpec::tpl("ascii({textCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "CHAR", EntrySpec::tpl("chr(CAST({0} AS INTEGER))", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "SIGN", EntrySpec::tpl("sign({numericCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "TRUNC", EntrySpec::tpl("trunc({numericCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "ROUND", EntrySpec::tpl("round({numericCast:0}, CAST({1} AS INTEGER))", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "MIN", EntrySpec::tpl("least({*})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "MAX", EntrySpec::tpl("greatest({*})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "POWER", EntrySpec::tpl("power({numericCast:0}, {numericCast:1})", "NUM").caveat("numeric-scale"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "ISNUM", EntrySpec::tpl("({textCast:0} ~ '^-?[0-9]+(\\.[0-9]+)?$')", "BOOL"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "BLEN", EntrySpec::tpl("octet_length({binaryCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "TO_UTF8", EntrySpec::tpl("{binaryCast:0}", "BIN"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "FROM_UTF8", EntrySpec::tpl("convert_from({binaryCast:0}, 'UTF8')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "TO_HEX", EntrySpec::tpl("encode({binaryCast:0}, 'hex')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "FROM_HEX", EntrySpec::tpl("decode({0}, 'hex')", "BIN"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "ENCODE_BASE64", EntrySpec::tpl("replace(replace(encode({binaryCast:0}, 'base64'), chr(10), ''), chr(13), '')", "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "DECODE_BASE64", EntrySpec::tpl("decode({0}, 'base64')", "BIN").caveat("input-laxity"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "CRC32", EntrySpec::withdraw("PostgreSQL has no CRC32 in core; pgcrypto supplies digests but not CRC32"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "RMATCH", EntrySpec::tpl("({textCast:1}{textCollate} ~ {0})", "BOOL").caveat("regex-engine").arity(2, 2));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "RFIND", EntrySpec::tpl("regexp_instr({textCast:1}{textCollate}, {0})", "NUM").caveat("regex-engine").arity(2, 2));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "UPPER", EntrySpec::tpl("upper({textCast:0})", "TEXT").caveat("unicode-case"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "LOWER", EntrySpec::tpl("lower({textCast:0})", "TEXT").caveat("unicode-case"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "SUBSTR", EntrySpec::by_count({{"2", "SUBSTRING({textCast:0} FROM CAST({1} AS INTEGER))"}, {"3", "SUBSTRING({textCast:0} FROM CAST({1} AS INTEGER) FOR CAST({2} AS INTEGER))"}}, "TEXT"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "ABS", EntrySpec::tpl("abs({numericCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "CEIL", EntrySpec::tpl("ceil({numericCast:0})", "NUM"));
  ++calls;
  Map::define("postgresql~replay", Section::Funcs,
              "FLOOR", EntrySpec::tpl("floor({numericCast:0})", "NUM"));
  ++calls;
  Map::define_dialect("sqlite~replay",
                      DialectSpec::extending("ansi~replay").version("3.35").target(true).lexical("identQuote", "\"").lexical("identEscape", "\"\"").lexical("textQuote", "'").lexical_escapes("textEscape", {{"'", "''"}}).lexical("true", "1").lexical("false", "0").lexical("binaryLiteral", "x'{hex}'").lexical("numericLiteral", "'{0}'").lexical("textCollate", "").lexical("textCast", "CAST({0} AS TEXT)").lexical("numericCast", "CAST({0} AS NUMERIC)").lexical("binaryCast", "CAST({0} AS BLOB)").lexical("isTrue", "(({0}) IS TRUE)").lexical("isNotTrue", "(({0}) IS NOT TRUE)").lexical("placeholder", "?"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "+", EntrySpec::tpl("({0} + {1})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "-", EntrySpec::tpl("({0} - {1})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "*", EntrySpec::tpl("({0} * {1})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "/", EntrySpec::tpl("({0} * 1.0 / {1})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "%", EntrySpec::tpl("({0} % {1})", "NUM").caveat("modulo-integer"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "NEG", EntrySpec::tpl("(-{0})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "==", EntrySpec::variants({{"num", "({numericCast:0} = {numericCast:1})"}, {"coerce", "({numericCast:0} = {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "!=", EntrySpec::variants({{"num", "({numericCast:0} <> {numericCast:1})"}, {"coerce", "({numericCast:0} <> {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "<", EntrySpec::variants({{"num", "({numericCast:0} < {numericCast:1})"}, {"coerce", "({numericCast:0} < {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "<=", EntrySpec::variants({{"num", "({numericCast:0} <= {numericCast:1})"}, {"coerce", "({numericCast:0} <= {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              ">", EntrySpec::variants({{"num", "({numericCast:0} > {numericCast:1})"}, {"coerce", "({numericCast:0} > {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              ">=", EntrySpec::variants({{"num", "({numericCast:0} >= {numericCast:1})"}, {"coerce", "({numericCast:0} >= {numericCast:1})"}}, "BOOL").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "&", EntrySpec::variants({{"text", "({0} || {1})"}, {"bin", "({0} || {1})"}}, "@concat"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "BAND", EntrySpec::withdraw("SQLite's & is integer-only and has no meaning over blobs"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "BOR", EntrySpec::withdraw("SQLite's | is integer-only and has no meaning over blobs"));
  ++calls;
  Map::define("sqlite~replay", Section::Ops,
              "BXOR", EntrySpec::withdraw("SQLite has no XOR operator at all, over integers or blobs"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "UPPER", EntrySpec::tpl("upper({0})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "LOWER", EntrySpec::tpl("lower({0})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "LEN", EntrySpec::tpl("length({0})", "NUM"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "LEFT", EntrySpec::tpl("substr({0}, 1, {1})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "RIGHT", EntrySpec::tpl("substr({0}, -{1}, {1})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "SUBSTR", EntrySpec::by_count({{"2", "substr({0}, {1})"}, {"3", "substr({0}, {1}, {2})"}}, "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "FIND", EntrySpec::tpl("instr({1}, {0})", "NUM").arity(2, 2));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "REPLACE", EntrySpec::tpl("replace({2}, {0}, {1})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "TRIM", EntrySpec::tpl("trim({0}, ' \011\015\012')", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "LTRIM", EntrySpec::tpl("ltrim({0}, ' \011\015\012')", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "RTRIM", EntrySpec::tpl("rtrim({0}, ' \011\015\012')", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "BACKWARDS", EntrySpec::withdraw("SQLite has no reverse(); reversing text needs a recursive CTE, which is a statement rather than an expression"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "REPEAT", EntrySpec::withdraw("SQLite has no repeat(); printf can pad with one character but cannot repeat a string"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "PADL", EntrySpec::withdraw("SQLite has no LPAD; printf('%*s') pads with spaces only and cannot take SEL's fill string"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "PADR", EntrySpec::withdraw("SQLite has no RPAD; printf('%-*s') pads with spaces only and cannot take SEL's fill string"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "CODE", EntrySpec::tpl("unicode({0})", "NUM"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "CHAR", EntrySpec::tpl("char({0})", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "SIGN", EntrySpec::tpl("sign({0})", "NUM"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "ABS", EntrySpec::tpl("abs({0})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "CEIL", EntrySpec::tpl("ceil({0})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "FLOOR", EntrySpec::tpl("floor({0})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "TRUNC", EntrySpec::tpl("trunc({0})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "ROUND", EntrySpec::tpl("round({0}, {1})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "MIN", EntrySpec::tpl("min({*})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "MAX", EntrySpec::tpl("max({*})", "NUM").caveat("decimal-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "POWER", EntrySpec::tpl("pow({0}, {1})", "NUM").caveat("power-float"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "ISNUM", EntrySpec::withdraw("SQLite has no REGEXP, and CAST answers 0 for 'abc' rather than saying it is not a number, so there is no expression that asks SEL's question"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "BLEN", EntrySpec::tpl("length({binaryCast:0})", "NUM"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "TO_UTF8", EntrySpec::tpl("CAST({0} AS BLOB)", "BIN"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "FROM_UTF8", EntrySpec::tpl("CAST({binaryCast:0} AS TEXT)", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "TO_HEX", EntrySpec::tpl("lower(hex({binaryCast:0}))", "TEXT"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "FROM_HEX", EntrySpec::withdraw("unhex() arrived in SQLite 3.41 and this document targets 3.35; there is no portable expression that builds a blob from hex text"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "ENCODE_BASE64", EntrySpec::withdraw("SQLite has no base64 function in a default build"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "DECODE_BASE64", EntrySpec::withdraw("SQLite has no base64 function in a default build"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "CRC32", EntrySpec::withdraw("SQLite has no CRC32 function in a default build"));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "RMATCH", EntrySpec::withdraw("SQLite has no REGEXP function unless the application registers one; a default build raises \"no such function: REGEXP\""));
  ++calls;
  Map::define("sqlite~replay", Section::Funcs,
              "RFIND", EntrySpec::withdraw("SQLite has no REGEXP function unless the application registers one; a default build raises \"no such function: REGEXP\""));
  ++calls;
  Map::define_dialect("mariadb~replay",
                      DialectSpec::extending("mysql-family~replay").version("10.5").target(true));
  ++calls;
  Map::define("mariadb~replay", Section::Skel,
              "case", EntrySpec::skeleton("CASE {branches} ELSE {else} END").caveat("numeric-scale"));
  ++calls;
  return calls;
}

}  // namespace sel::sqlt
