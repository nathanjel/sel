# Review of commit ed16df2 — index

Swarm review of `git diff 8fe0e3a ed16df2` run on 2026-09-14/15: eight review lenses produced 74 reports, deduplicated into 29 findings; each finding was handed to one independent agent acting as skeptic and reproducer (own program, every relevant host and lane), which also classified it as introduced by the commit, pre-existing, or mixed. One file per finding, `review-2026-09-15-NN-<id>-*.md`; each carries the verdict, the verifier's summary, reasoning and evidence, the suggested case-first fix, the original lens reports, and the verifier's reproduction scripts and command transcript so they can seed conformance / .sqlt / unit cases.

Verdicts: 28 CONFIRMED, 1 PARTIAL, 0 REFUTED. Two further findings come from the completeness critic (not verified by a second agent).

| # | id | verdict | sev | introduced | hosts | finding |
|---|---|---|---|---|---|---|
| | | | | | | **Introduced or promised by ed16df2** |
| 01 | AA | CONFIRMED | high | by-this-commit | php, js, python, lisp, cpp | [PHP physicalAst() is not keyed by the identity of $ast: reassigning $program->ast is not noticed](review-2026-09-15-01-AA-php-physicalast-is-not-keyed-by-the-identity-of-ast-reassign.md) |
| 02 | I | PARTIAL | high | mixed | js, php, python, cpp, lisp | [A pipeline that ends in a bare or sealed bucket is classified pure_sql: the full-pushdown probe skips bucketRowsAreKeys](review-2026-09-15-02-I-a-pipeline-that-ends-in-a-bare-or-sealed-bucket-is-classifie.md) |
| 03 | AI | CONFIRMED | high | mixed | js, python, php, cpp, lisp | [A second BUCKET after a bare bucket re-opens the sealed plan: BUCKET .> BUCKET .> MAP is translated instead of refused](review-2026-09-15-03-AI-a-second-bucket-after-a-bare-bucket-re-opens-the-sealed-plan.md) |
| 04 | D | CONFIRMED | high | mixed | php, js, python | [PHP planHybrid ignores foldConstants:false](review-2026-09-15-04-D-php-planhybrid-ignores-foldconstants-false.md) |
| 05 | A | CONFIRMED | high | mixed | js, php, python, cpp, lisp | [IF fold to a text literal changes SORT_BY/TOP_BY form resolution](review-2026-09-15-05-A-if-fold-to-a-text-literal-changes-sort-by-top-by-form-resolu.md) |
| 06 | J | CONFIRMED | high | mixed | js, php, python, cpp, lisp | [Bucket projection and HAVING accept bodies SEL refuses or lacks (member fields via the group binder, later-MAP aliases, MIN/MAX(g, body), two-argument SUM with _)](review-2026-09-15-06-J-bucket-projection-and-having-accept-bodies-sel-refuses-or-la.md) |
| 07 | K | CONFIRMED | high | mixed | js, php, python, cpp, lisp | [_K outside the bucket body (before the BUCKET, after its MAP) is rendered as the group key where SEL binds the list index](review-2026-09-15-07-K-k-outside-the-bucket-body-before-the-bucket-after-its-map-is.md) |
| 08 | G | CONFIRMED | high | mixed | js, php, python, cpp, lisp | [BUCKET(k) .> MAP(proj) is not BUCKET(k, proj) in the evaluator for NULL/BOOL/LIST/RECORD keys; the bare BUCKET stringifies keys differently per host](review-2026-09-15-08-G-bucket-k-map-proj-is-not-bucket-k-proj-in-the-evaluator-for-.md) |
| 09 | P | CONFIRMED | high | mixed | js, php, python, cpp, lisp | [MAP fall-through pushes shape-changing steps after the MAP and re-evaluates the original RECORD over projected rows](review-2026-09-15-09-P-map-fall-through-pushes-shape-changing-steps-after-the-map-a.md) |
| 10 | AJ | CONFIRMED | high | mixed | js, python, php, cpp, lisp | [Hybrid continuation reports an inlined helper at its definition-site column while run() reports the use site](review-2026-09-15-10-AJ-hybrid-continuation-reports-an-inlined-helper-at-its-definit.md) |
| 11 | U | CONFIRMED | high | mixed | python, cpp, js, php, lisp | [A stage-1 clist in the planned tree and TAKE(1/0): host-native exceptions escape translate_statement and plan_hybrid in JS/PHP/Python](review-2026-09-15-11-U-a-stage-1-clist-in-the-planned-tree-and-take-1-0-host-native.md) |
| 12 | C | CONFIRMED | high | mixed | lisp, js, python, php, cpp | [translate()/translate_statement apply different optimisation policies per host](review-2026-09-15-12-C-translate-translate-statement-apply-different-optimisation-p.md) |
| | | | | | | **Pre-existing, but contradicting a promise ed16df2 wrote** |
| 13 | R | CONFIRMED | high | pre-existing | cpp, js, php, python, lisp | [LAZY_RECORD: C++ MAP clones rows and forces unread lazy fields; the other four never evaluate an unread MAP field](review-2026-09-15-13-R-lazy-record-c-map-clones-rows-and-forces-unread-lazy-fields-.md) |
| 14 | AF | CONFIRMED | high | pre-existing | lisp, js, php, python, cpp | [Lisp optimiser raises E_DEPTH itself where the other four fold past the boundary the evaluator refuses](review-2026-09-15-14-AF-lisp-optimiser-raises-e-depth-itself-where-the-other-four-fo.md) |
| 15 | E | CONFIRMED | medium | pre-existing | js, php, python, cpp, lisp | [FILTER(TRUE) elimination is observable: FILTER over a scalar returns the scalar; a pipeline reduced to its bare source plans as pure_memory](review-2026-09-15-15-E-filter-true-elimination-is-observable-filter-over-a-scalar-r.md) |
| | | | | | | **Pre-existing cross-host divergences surfaced by the review** |
| 16 | B | CONFIRMED | high | pre-existing | php, cpp, lisp, js, python | [Unary-minus fold arm is dead in PHP, C++ and Lisp (tests "-", the parser emits NEG)](review-2026-09-15-16-B-unary-minus-fold-arm-is-dead-in-php-c-and-lisp-tests-the-par.md) |
| 17 | C2 | CONFIRMED | high | pre-existing | js, php, python, cpp, lisp | [MAP/SORT swap in the logical optimiser changes the program value; runs inside translate() in JS/Python only](review-2026-09-15-17-C2-map-sort-swap-in-the-logical-optimiser-changes-the-program-v.md) |
| 18 | F | CONFIRMED | high | pre-existing | lisp | [Lisp translator/planner calls the undefined function SNODE-R on sealed-bucket shapes](review-2026-09-15-18-F-lisp-translator-planner-calls-the-undefined-function-snode-r.md) |
| 19 | H | CONFIRMED | high | pre-existing | js, php, python, cpp, lisp | [FILTER after a grouped plan that already carries LIMIT/OFFSET/ORDER BY: Lisp wraps (WHERE), the other four append HAVING before LIMIT](review-2026-09-15-19-H-filter-after-a-grouped-plan-that-already-carries-limit-offse.md) |
| 20 | L | CONFIRMED | high | pre-existing | js, php, python, cpp, lisp | [GROUP BY over a TEXT key is emitted without the binary collation applied to $==](review-2026-09-15-20-L-group-by-over-a-text-key-is-emitted-without-the-binary-colla.md) |
| 21 | Q | CONFIRMED | high | pre-existing | cpp | [C++ BUCKET binds _K in the key expression to the positional index; the other four bind the source key](review-2026-09-15-21-Q-c-bucket-binds-k-in-the-key-expression-to-the-positional-ind.md) |
| 22 | S | CONFIRMED | high | pre-existing | lisp | [Lisp MAP fall-through emits the dependency columns in reverse order](review-2026-09-15-22-S-lisp-map-fall-through-emits-the-dependency-columns-in-revers.md) |
| 23 | T | CONFIRMED | high | pre-existing | lisp, php, cpp, js, python | [Fall-through classification diverges: Lisp walkers skip list nodes and accept only RECORD bodies; LAZY_RECORD is SQL-special in PHP/C++ only](review-2026-09-15-23-T-fall-through-classification-diverges-lisp-walkers-skip-list-.md) |
| 24 | V | CONFIRMED | high | pre-existing | lisp, js, php, python, cpp | [Lisp applies logical rewrites as per-rule passes; SORT_BY .> SORT_BY / SORT_BY .> TAKE reach the translator differently and render a wrong ORDER BY](review-2026-09-15-24-V-lisp-applies-logical-rewrites-as-per-rule-passes-sort-by-sor.md) |
| 25 | W1 | CONFIRMED | high | pre-existing | lisp, js, python, php, cpp | [Lisp keeps bound column kinds through a pass-through derived table; the other four degrade them to UNKNOWN](review-2026-09-15-25-W1-lisp-keeps-bound-column-kinds-through-a-pass-through-derived.md) |
| 26 | W2 | CONFIRMED | high | pre-existing | lisp, cpp, js, php, python | [Other translator divergences the planner turns into different plans (Lisp DISTINCT/ORDER BY wrap, C++ derived column spelling, LINK binder scoping)](review-2026-09-15-26-W2-other-translator-divergences-the-planner-turns-into-differen.md) |
| 27 | X | CONFIRMED | high | pre-existing | js, php, python, cpp, lisp | [COUNT translation: COUNT(_) in a non-grouped MAP becomes COUNT(*); COUNT over a BUCKET/LIST/RECORD/DISTINCT/SORT_BY/TAKE result folds to literal 0](review-2026-09-15-27-X-count-translation-count-in-a-non-grouped-map-becomes-count-c.md) |
| 28 | Y | CONFIRMED | high | pre-existing | js, lisp, php, python, cpp | [In-memory LINK builds rows with duplicate keys in JS and Lisp but not PHP/Python/C++](review-2026-09-15-28-Y-in-memory-link-builds-rows-with-duplicate-keys-in-js-and-lis.md) |
| 29 | AK | CONFIRMED | medium | pre-existing | lisp | [Lisp execute-hybrid hands the runner inline-mode SQL and creation-order params; the other four hand placeholder SQL and bindings()](review-2026-09-15-29-AK-lisp-execute-hybrid-hands-the-runner-inline-mode-sql-and-cre.md) |
| | | | | | | **From the completeness critic** |
| 30 | CRITIC | unverified | medium | — | php | [tools/check.sh never runs the PHP optimiser check under the default roster (first-match `case`)](review-2026-09-15-30-CRITIC-tools-check-sh-never-runs-the-php-optimiser-check-under-the-.md) |
| 31 | CRITIC | unverified | medium | — | js, php, python, cpp, lisp | [Neither fuzzer nor the mutation catalogue can reach the pipeline/planner code the commit changes](review-2026-09-15-31-CRITIC-neither-fuzzer-nor-the-mutation-catalogue-can-reach-the-pipe.md) |
| 32 | CRITIC | unverified | low | — | js | [JS translator still carries a GROUP_BY arm and GROUP_BY-worded refusals after the rename](review-2026-09-15-32-CRITIC-js-translator-still-carries-a-group-by-arm-and-group-by-word.md) |

## Gaps the critic found no lens covered

- tools/api.mjs / tools/api.php etc. (tools/check-api.sh) probe neither physicalAst()/physical_ast()/program-physical-ast nor any of plan_hybrid's nine fields, so the 54-probe API-parity lane is green without seeing the surface this commit added
- No conformance case pins that GROUP_BY is now E_UNKNOWN_FUNC (all five agree at 1:24 today; nothing stops a host re-registering the alias)
- spec/SPEC.md and docs/LANGUAGE.md never mention BUCKET (or LINK/SORT_BY/TAKE as pipeline steps); the evaluator semantics the translator fold must match (findings G/H/K) exist only in conformance/16-bucket.selt and docs/SQL-TRANSLATION.md, so 'spec decides when hosts disagree' has nothing to decide with
- cpp `make asan` compiles only tests/unit.cpp and bin/conformance.cpp; the SQL layer and planner (sel_sql_hybrid.cpp, unwind/build_pipeline, bucket state, call_once physical cache) have no sanitizer target — I built one by hand and it is clean, but nothing in the tree will re-run it
- sql/mutations.json has no planner, bucket or hybrid mutation and no lens (nor I, beyond one hand mutation) ran tools/mutate-sql.sh
- Neither fuzzer reaches the pipeline lanes: tools/gen-programs.mjs (unchanged by the commit) emits no `.>`, BUCKET, SORT_BY, TAKE, LINK or 3-arg SORT_BY, so tools/fuzz.sh and tools/fuzz-sql.sh cannot find any of the ~30 pipeline findings above, and the EXTENDING guardrail 'the fuzzer compares positions, and so must any fold you add' covers scalar folds only
- Statement-level SQL (GROUP BY/HAVING/derived tables) has no oracle: sql/oracle covers expressions only and tools/check-sql-oracle.sh skips without a DSN; only lens L ran a live server, for one shape
- python-wheel roster entry not run by anyone; dist/python/sel_lang-0.7.4 wheel is from Sep 9 and predates the commit's python/ changes (guarded by impls.sh staleness check, not in the default roster)
- Binding.raw, relation `scalar`/`correlate`/`prefilter` bindings and Binding.columns() under plan_hybrid/execute_hybrid were exercised by no lens
- Multi-line programs (line > 1 positions) through the hybrid continuation were probed only via REPLs, not via execute_hybrid
- CHANGELOG's measured gains (x1.5 JS, x3.5 Python, x1.04 Lisp) were re-measured by nobody
- CLAUDE.md/EXTENDING doc drift not pinned by any check: 'JS and PHP have none' (unit tests) and check.sh's 'no separate unit tests' comment are stale now that tools/check-js-optimizer.mjs / check-php-optimizer.php exist; EXTENDING's `cd cpp && make builds build/{...unit}` omits sqlunit/sqlt; 'Deep copies happen at exactly five sites' is contradicted by finding R
- The sqlt runners' 'mirrored dialect' pass for plan cases (which dialect pairs, whether tables/continuation are re-asserted on the mirror) was not read by any lens
- tools/check.sh itself was not run by anyone (explicitly forbidden); its step list was only read

## Low-severity reports not sent to verification

- **[low/coherence] isSameField folds alias case with the host's native case rules in JS and Lisp, ASCII in the other three** (bucket-translator; js, lisp, php, python, cpp) — The new shared bucketProjection helper decides whether a projection is an aggregate alias by comparing alias and field name case-insensitively: JS uses String.prototype.toUpperCase (Unicode: 'straße' -> 'STRASSE'), Lisp uses string-equal (Unicode char-equal), PHP strtoupper (ASCII since 8.2), Python and C++ ascii_upper. For a non-ASCII alias the hosts disagree on whether an aggregateAliases entry is recorded, and so on how a later HAVING/ORDER BY reference to that alias renders. CLAUDE.md forbids native case handling for anything the language defines. Transcribed from the pre-existing BUCKET branch, so today's fixtures (ASCII aliases) cannot show it.
- **[low/doc-claim] JS translator still carries a dead GROUP_BY case and GROUP_BY-worded refusals after the rename** (bucket-translator; js) — CHANGELOG: 'every translator now carr[ies] BUCKET alone'. js/src/sql/translator.mjs keeps `case 'GROUP_BY':` on the BUCKET arm and refuses with 'the binder of GROUP_BY must be a bare name' / 'GROUP_BY takes 2 to 4 arguments'. It is unreachable (PIPELINE_OPS from optimizer.mjs:9 has no GROUP_BY and the registry no longer defines it), so it is dead code and stale message text, not a behavioural divergence; the other four hosts say BUCKET.
- **[low/doc-claim] JS translator still carries a dead `case 'GROUP_BY':` label and GROUP_BY-worded refusals; 'every translator now carries BUCKET alone' is overstated** (promises-vs-code; js) — The CHANGELOG says GROUP_BY is gone from 'the registries, dependencies() and every translator'. It is gone from PIPELINE_OPS (so the label is unreachable), from all registries, from dependencies() in all five hosts (verified: six BUCKET shapes give identical --deps output on every REPL), from generated case data and from dist/. But js/src/sql/translator.mjs keeps `case 'GROUP_BY':` and two user-facing messages naming GROUP_BY where Python, PHP, C++ and Lisp say BUCKET. Messages are not asserted, so no test notices; a grep for GROUP_BY (the check the CHANGELOG invites) does.
- **[low/coherence] Lisp leaves join columns unqualified when fields declare no table (pre-existing, outside the diff, surfaces in LINK-before-BUCKET)** (bucket-translator; lisp) — With relation fields that carry no `table`, JS/PHP/Python/C++ qualify every column by the relation's table or alias once a join is present (`items`.`dept`, `d`.`name`), Lisp emits bare `dept`, `name` — including in the GROUP BY and the bucket projection. Different SQL text, and ambiguous-column errors on a real engine when both tables share a column name. Not introduced by this commit and independent of BUCKET (a plain LINK shows it), noted because the lens asked for LINK before/after a bucket.
- **[low/coherence] Minor structural divergences in the planners that today coincide on results** (hybrid-planner; lisp) — (1) Lisp's fall-through takes any 3-argument MAP as explicit-binder and reads node-s of the second argument without checking it is an ungrouped :var; the other four require `args[1].t === 'var' && !grouped`. Today the translator refuses the grouped form so the plan ends pure_memory either way; a translator that accepted `MAP((X), body)` would make Lisp split where the others do not. (2) Lisp's execute-hybrid builds the continuation context by copying the caller's keys one by one into a fresh value (aliasing the caller's children) where JS/PHP/Python/C++ clone the context; a continuation that mutated a context value would write into the caller's tree in Lisp only. (3) Lisp stamps the synthesised dependency key/index nodes with the RECORD's position (copy-node rec-node) while the others use the MAP step's position; only visible if a translation error is reported on those nodes.
- **[medium/test-gap] check.sh silently skips the Python unit lane when system python3 lacks pytest, hiding the only DB-executed bucket check and the Python planner-contract tests** (runners-generator-tests; python) — impl_unit python runs `python3 -c 'import pytest'` and returns 0 with a 'unit tests skipped' message when it fails; on this machine system python3 has no pytest (only .venv does), so tools/check.sh's default roster never runs python/tests. That lane holds tests the other hosts do not have: test_bucket_plans_answer_what_the_evaluator_answers_on_sqlite (the only executed-plan comparison against a real database, cited by the CHANGELOG), test_planner_options_reach_the_logical_optimiser, test_program_caches_the_physical_ast_per_source_tree. The remediation plan's final gate states 'a missing test dependency is an infrastructure failure, not a passing result', and the gate still reports ALL GREEN without them. (I replayed the 11 bucket shapes through executeHybrid in JS/PHP/Lisp/C++ against a sqlite oracle and all agree today, so this is a gate gap, not a present divergence.)
- **[low/test-gap] Host-local unit coverage is not equivalent across hosts (matrix); C++ has no logical-fold position checks, only Python executes bucket plans, PHP lacks the cache-key check** (runners-generator-tests; cpp, php, js, lisp, python) — Matrix of the checks the commit adds (Y = present): helper-assignment normalised + physical source tables: JS Y, PHP Y, PY Y, Lisp Y, C++ Y. fuseFilters reaches optimiser: JS Y, PHP Y, PY Y (Lisp/C++ n/a). foldConstants reaches optimiser: none. stage-1-refused => pure_memory over original: all Y. AST unchanged across run+optimisers+plan: JS Y, PHP Y (vacuous — PHP arrays are values, Optimizer::optimize takes the array by value so the check can never fail), PY Y, Lisp Y, C++ by type. physical built once: all Y. reassigning ast drops cache: JS Y, PY Y, Lisp Y, PHP N (and behaves differently), C++ n/a. logical-fold positions (IF hoist col 5, AND col 12, OR col 11, compound/variable branch not folded): JS Y, PHP Y, PY Y, Lisp Y, C++ N — C++ relies on the conformance suite and the three planner probes; `IF(TRUE, X, 2)` staying an IF under optimize_ast_logical is checked nowhere in C++. continuation reports errors where run() does (3 probes): all Y. bucket plans executed against a DB: PY only. Only the PHP row hides a real behavioural difference (separate finding); the rest is where a future regression in one host would pass its local tests.
- **[low/coherence] A refused plan's `--- error` position is accepted by the generator but ignored by all five runners** (runners-generator-tests; js, php, python, cpp, lisp) — For ordinary cases every runner compares the `CODE line:col` position when one is written; for a `--- plan refused` case every runner parses the same ERROR_RE/parse_expected form but compares only the code (Lisp explicitly `(declare (ignore line col))`). The generator's PLANS validation does not reject a position in a refused plan's `--- error`, so a future case such as `E_SQL_BINDING 1:9` would pass in all hosts regardless of the position reported. Consistent across the five hosts today (the one refused case, plan.refuse.base-dialect, carries no position), but a silent no-op assertion in a format whose whole point is that assertions mean the same thing everywhere.
- **[low/doc-claim] Fixture notes and CHANGELOG claim more than the assertions check** (runners-generator-tests; js, php, python, cpp, lisp) — (1) plan.pure-sql.filters-fuse-before-planning's note says 'The planner's options object reaches that optimiser as well as the translator in every host that has optimiser options', but the case passes no `--- options`; the assertion only shows the default fuses, and the fixture format cannot express a non-strict option portably (the C++ and Lisp emitters carry `strict` alone, tools/gen-sql-cases.mjs emitCpp `.strict` / emitLispCases `:strict`). Option forwarding is checked only by the host-local tests, and only for fuseFilters (see the foldConstants finding). (2) op.logic.not-over-a-folded-and-keeps-the-not-position's note says NOT 'produces its own, at 1:1' while the source `(NOT (FALSE AND TRUE)) + 1` has NOT at column 2 and the expectation (correctly) says 1:2. (3) CHANGELOG says 25-hybrid-plans.sqlt 'holds 25 planner cases'; it holds 33 (`grep -c '^--- plan'`).
- **[low/doc-claim] Smaller doc/claim mismatches: '25 planner cases' (file has 33), BUCKET defined nowhere normative, EXTENDING 'Where everything lives' has no optimiser/planner rows** (promises-vs-code; js, python, php, cpp, lisp) — (a) CHANGELOG first group says 25-hybrid-plans.sqlt 'holds 25 planner cases'; it holds 33 after the fold and bucket groups were added (grep -c '^### name' = 33). (b) CLAUDE.md and EXTENDING state 'The grouping verb is BUCKET', but BUCKET (like LINK, LINK_LEFT, TOP*, SORT_BY, the whole `.>` pipeline vocabulary) appears in neither spec/SPEC.md §7 nor docs/LANGUAGE.md nor README.md — the spec-first rule has no spec entry for the verb the commit renames, and 'spec/ and conformance/ decide' cannot decide the bucket-key semantics in the evaluator finding above. (c) The EXTENDING 'Where everything lives' table (docs/EXTENDING.md:50-62) still has no row for optimizer.* or sql/hybrid.*, although the new section tells contributors those are the two hand-transcribed walks. The 'Measured ×1.5 JS and ×3.5 Python' figures are stated as measurements, not guarantees, and are not flagged.
- **[low/coherence] C++ optimiser rewrites assignment targets; the other four leave the target untouched** (program-cache-immutability; cpp, js, php, python, lisp) — In the non-pipeline arm JS, PHP and Python special-case `assign` to optimise only `value` (with a comment explaining that targets are walked iteratively by the evaluator and must not be charged), and Lisp's optimize-children optimises only node-r for :assign. C++ opt_tree has no such arm: it does `if (copy->l) copy->l = opt_tree(copy->l, ...)` for every node kind, and for NT::Assign `l` is the target (cpp/sel.cpp:2013-2014), so index keys inside a target are folded, pipelines inside a target get TOP fusion, join pushdown and LAZY_RECORD, and IF hoisting in C++ only. Today this produces the same outputs (verified: folded/hoisted keys, SORT+TAKE→TOP, FILTER fusion, MAP→LAZY_RECORD and foldable 195–201-term chains inside `R[...] = 1` agree across all five), because every current rewrite is semantics- and position-preserving and C++ stops at depth > 200 rather than raising. It becomes visible with the first rewrite that is observable inside a target — e.g. if the C++ optimiser adopted the Lisp-style E_DEPTH raise (finding 1) it would fire inside targets where no other host looks, or any future rewrite that changes forcing order/laziness of a value used as a key. One guard order in five hosts is the point of the remediation; C++ should skip the target like the others.
- **[low/doc-claim] C++ comment on the once-built physical cell describes a guard the C++ optimiser does not have** (program-cache-immutability; cpp) — The Physical struct comment says call_once 'lets a throwing build (E_DEPTH from the optimiser's own guard) be retried rather than cached as absent'. The C++ optimiser has no throwing depth guard: opt_tree returns the node unrewritten at depth > MAX_DEPTH (cpp/sel.cpp:5845) and every fold wraps decimal work in try/catch, so optimize_ast cannot raise E_DEPTH on a compiled tree. The behaviour described is Lisp's (finding 1). The call_once retry semantics are correct as far as they go, but the comment documents the wrong host and would lead a maintainer to expect the C++ build to raise on deep programs — the opposite of what it does, and the opposite of what the four folding hosts do.
- **[low/coherence] Refusal code for indexing a group by position differs: E_SQL_BINDING (py/js/php) vs E_SQL_SHAPE (cpp/lisp)** (probe-lanes-bucket-hybrid; python, js, php, cpp, lisp) — `_[1]["amount"]` inside a bucket projection is refused at the same position by every host but with two different codes and messages: the three dynamic hosts report E_SQL_BINDING "unknown joined relation '1'" (the numeric index falls into the join-alias lookup), C++ and Lisp report E_SQL_SHAPE ("only a bound name can be indexed here" / "a row of a relation has no key"). Tests assert codes, and tools/fuzz-sql.sh compares codes across hosts, so this is a latent red fuzz result the moment the generator emits a positional index on a binder inside a bucket.

## Found during remediation (after the review)

| # | Finding | Status |
|---|---|---|
| 33 | [The FILTER-before-MAP rewrite renumbers the result's keys](review-2026-09-15-33-NEW-filter-before-map-rewrite-renumbers-keys.md) — found while fixing AJ and R; one witness per host, not independently verified | FIXED 2026-09-16 |
