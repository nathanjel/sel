--- 619bc31/cpp/sel.hpp
+++ inline-collection/cpp/sel.hpp
@@ -260,7 +260,7 @@
     bool boolean = false;
     bool is_list = false;
     mutable std::unique_ptr<Dec> decimal;
-    std::unique_ptr<Collection> collection;
+    std::optional<Collection> collection;
     Impl* next_free = nullptr;
 
     const Collection& coll() const {
@@ -270,7 +270,7 @@
       return empty;
     }
     Collection& mutable_coll() {
-      if (!collection) collection = std::make_unique<Collection>();
+      if (!collection) collection.emplace();
       return *collection;
     }
 
