--- 619bc31/cpp/sel.hpp
+++ inline-decimal/cpp/sel.hpp
@@ -259,7 +259,7 @@
     mutable bool scalar_computed = false;
     bool boolean = false;
     bool is_list = false;
-    mutable std::unique_ptr<Dec> decimal;
+    mutable std::optional<Dec> decimal;
     std::unique_ptr<Collection> collection;
     Impl* next_free = nullptr;
 
@@ -340,11 +340,11 @@
 inline void Value::set_dec(const Dec& d) const {
   if (p_) {
     if (p_->decimal) *p_->decimal = d;
-    else p_->decimal = std::make_unique<Dec>(d);
+    else p_->decimal = d;
   }
 }
 inline const Dec* Value::dec_val() const {
-  return p_ ? p_->decimal.get() : nullptr;
+  return p_ && p_->decimal ? &*p_->decimal : nullptr;
 }
 inline void Value::set_dec_val(std::shared_ptr<const Dec> d) const {
   if (d) set_dec(*d);
--- 619bc31/cpp/sel.cpp
+++ inline-decimal/cpp/sel.cpp
@@ -1262,7 +1262,7 @@
   static Value from_dec(Dec d) {
     Value v;
     v.p_->kind = Kind::Text;
-    v.p_->decimal = std::make_unique<Dec>(std::move(d));
+    v.p_->decimal = std::move(d);
     v.p_->scalar_computed = false;
     return v;
   }
@@ -1364,7 +1364,7 @@
   out.p_->scalar_computed = p_->scalar_computed;
   out.p_->boolean = p_->boolean;
   out.p_->is_list = p_->is_list;
-  if (p_->decimal) out.p_->decimal = std::make_unique<Dec>(*p_->decimal);
+  if (p_->decimal) out.p_->decimal = *p_->decimal;
   if (!p_->coll().shape && (!p_->is_list || p_->coll().storage.empty()) && p_->coll().children.empty()) {
     return out;
   }
