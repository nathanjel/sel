A row in the right table in `spec/SPEC.md` §7. If it can fail in a new way, add
the code to `spec/errors.md` — reuse an existing code unless the new condition
is genuinely distinct.

<!-- EXAMPLE-BEGIN -->
| `ORD_SUFFIX(n)` | TEXT — `n` with its English ordinal suffix; `n` must be a non-negative integer |
<!-- EXAMPLE-END -->
